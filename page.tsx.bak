import Link from "next/link";
import { Button } from "@/components/ui/Button";
import { auth } from "@/lib/auth";

export default async function Home() {
  const session = await auth();

  return (
    <div className="flex flex-col items-center justify-center min-h-[80vh] text-center space-y-8">
      <h1 className="text-6xl md:text-8xl font-bold text-leather-pop tracking-tight drop-shadow-lg">
        Sarhni
      </h1>
      <p className="text-xl md:text-2xl text-leather-accent/80 max-w-2xl leading-relaxed">
        The cozy space for honest, anonymous confessions. <br/>
        Connect with friends without the mask (or with it).
      </p>
      
      <div className="flex gap-4">
        {session?.user ? (
          <Link href="/dashboard">
            <Button className="text-xl px-8 py-4">Go to Dashboard</Button>
          </Link>
        ) : (
          <>
            <Link href="/register">
              <Button className="text-xl px-8 py-4">Get Started</Button>
            </Link>
            <Link href="/login">
              <Button variant="secondary" className="text-xl px-8 py-4">Login</Button>
            </Link>
          </>
        )}
      </div>
    </div>
  );
}
